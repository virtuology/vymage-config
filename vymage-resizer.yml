hello: world this is from the config server

eureka:
  client:
    serviceUrl:
      defaultZone: http://vymage-service-registry.cfapps.io/eureka/
  instance:
    hostname: ${vcap.application.uris[0]}
    nonSecurePort: 80
